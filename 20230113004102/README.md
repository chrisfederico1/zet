# Quck note on Shell comands

* To prevent your commands from showing up in history put a space in front of command
* Use `cd -` to go back to where you were.
* Use `!!` to repeat last command.

    #zettle #Shell #Commands
